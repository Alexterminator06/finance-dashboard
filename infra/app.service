[Unit]
Description=Finance Dashboard
After=network.target

[Service]
User=Alexterminator06
WorkingDirectory=/home/Alexterminator06/finance-dashboard
ExecStart=python3 -m streamlit run app/streamlit_app.py --server.port 8501 --server.headless true
Restart=always

[Install]
WantedBy=multi-user.target
